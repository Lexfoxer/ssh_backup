{
  "name": "ssh_backup",
  "version": "1.1.0",
  "description": "The application features: create a backup via SSH (mainly support for webhosts Cpanel)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lexfoxer",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "0.30.0",
    "scp2": "0.5.0",
    "simple-ssh": "0.9.0",
    "tress": "1.0.3"
  }
}
